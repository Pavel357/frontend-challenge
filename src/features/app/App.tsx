import { FC, ReactElement } from 'react';

import CatsList from '../catsList/CatsList';

const App: FC = (): ReactElement => {
    return (
        <>
            <CatsList />
        </>
    );
};

export default App;